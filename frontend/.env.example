# 前端环境变量模板：复制为 frontend/.env 生效（Next.js 自动加载，勿入库）

# ── 后端地址（默认本机 8000，Docker/远程部署时改）──
# 服务端（route.ts 的 SSE relay 上游）：
FASTAPI_URL=http://127.0.0.1:8000
# 客户端（浏览器直连后端的 API 基址）：
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# ── 批量总结 fetch 超时（毫秒）──
# 默认 1800000（30 分钟，多长视频批量任务耗时长）；API 较慢的平台可调大
# SUMMARY_TIMEOUT_MS=1800000
